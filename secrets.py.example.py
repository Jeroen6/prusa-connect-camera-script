# The camera fingerprint is a unique id, try `uuidgen` in your liunx shell or `[guid]::NewGuid()` in powershell
camera_fingerprint = "sample-uuid"

# This is your camera api key, creat this by adding a camera in prusa connect
prusa_camera_api_token = "your prusa connect camera token"

# This is the source of your image, for example `http://[octoprint]/webcam/?action=snapshot`
image_source_url = "http://[octoprint]/webcam/?action=snapshot"